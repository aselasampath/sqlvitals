<UserControl x:Class="SqlVitals.Installer.Views.FinishView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Border x:Name="Badge" Width="40" Height="40" CornerRadius="20" Background="{StaticResource SuccessSoft}">
                <TextBlock x:Name="BadgeIcon" Text="✓" FontSize="20" FontWeight="Bold" Foreground="{StaticResource Success}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock x:Name="Title" Style="{StaticResource PageTitle}" Margin="14,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock x:Name="Summary" Margin="0,14,0,18" />

        <Button x:Name="BtnLaunch" Content="Launch SqlVitals" Style="{StaticResource PrimaryButton}"
                HorizontalAlignment="Left" Margin="0,0,0,20" Click="Launch_Click" />

        <Border x:Name="NextStepsCard" Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock x:Name="NextStepsTitle" Style="{StaticResource SectionTitle}" Text="Next steps" />
                <StackPanel x:Name="NextSteps" />
            </StackPanel>
        </Border>

        <Button Content="Open setup log" Style="{StaticResource LinkButton}" HorizontalAlignment="Left"
                Margin="0,16,0,0" Click="OpenLog_Click" />
    </StackPanel>
</UserControl>
